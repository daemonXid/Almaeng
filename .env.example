# ALMAENG Environment Configuration (PRD v2)
# Copy to .env and modify as needed

# --- App ---
DEBUG=true
SECRET_KEY=django-insecure-change-me-in-production

# --- Production Security (set DEBUG=false first) ---
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
# Comma-separated list of trusted origins for CSRF (required for production)
CSRF_TRUSTED_ORIGINS=https://your-domain.com
# Set to false if Cloudflare/Nginx handles SSL
SECURE_SSL_REDIRECT=false

# --- Ports ---
# APP_PORT: Django/Granian server (default: 8000)
# DB/Redis use standard ports internally, mapped via Docker
APP_PORT=3265

# --- Database ---
# These will be auto-replaced by init_project.py
POSTGRES_DB=almaeng_db
POSTGRES_USER=almaeng_user
POSTGRES_PASSWORD=almaeng_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5467

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6456

# === External APIs (PRD v2) ===

# --- AI (Google Gemini - google-genai SDK) ---
# Uses: google-genai (new unified SDK)
# Model: gemini-2.0-flash
GEMINI_API_KEY=

# --- Shopping APIs ---
# 11번가 Open API
# Docs: https://openapi.11st.co.kr/openapi/OpenApiGuide.tmall
ELEVENST_API_KEY=

# Naver Shopping Search API
# Docs: https://developers.naver.com/docs/serviceapi/search/shopping/shopping.md
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# --- Payments (Toss Payments V2 Widget) ---
# Docs: https://docs.tosspayments.com/guides/v2/get-started
TOSS_CLIENT_KEY=
TOSS_SECRET_KEY=

# --- Superuser (Auto-created on first run) ---
DJANGO_SUPERUSER_USERNAME=daemon
DJANGO_SUPERUSER_EMAIL=admin@daemon.local
DJANGO_SUPERUSER_PASSWORD=daemonkorea2026

# --- OAuth (Social Login) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# --- Email (SMTP) ---
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=

# --- Monitoring ---
SENTRY_DSN=
LOGFIRE_TOKEN=

# --- Security (Cloudflare Turnstile) ---
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- Storage (Cloudflare R2) ---
# R2_BUCKET=
# R2_ACCESS_KEY=
# R2_SECRET_KEY=
# R2_ENDPOINT_URL=

# === Architecture Note (PRD v2) ===
# AI 쇼핑 도우미 - 자연어 검색 기반 가격 비교 서비스
# - Gemini AI: 자연어 → 검색 키워드 추출
# - 11번가 + 네이버 쇼핑: 실시간 가격 비교
# - Toss Pay: 원클릭 결제
# - HTMX + Alpine.js: 반응형 UI
