# ALMAENG Environment Configuration
# Copy to .env and modify as needed

# --- App ---
DEBUG=true
SECRET_KEY=dlt5s$a^x+#ukz)g@^rx5)pv+50lg^pft3soil^w9#k#b#er3b

# --- Production Security (set DEBUG=false first) ---
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
# Comma-separated list of trusted origins for CSRF (required for production)
CSRF_TRUSTED_ORIGINS=https://your-domain.com
# Set to false if Cloudflare/Nginx handles SSL
SECURE_SSL_REDIRECT=false

# --- Ports ---
# APP_PORT: Django/Granian server (default: 8000)
# DB/Redis use standard ports internally, mapped via Docker
APP_PORT=5906

# --- Database ---
# These will be auto-replaced by init_project.py
POSTGRES_DB=almaeng_db
POSTGRES_USER=almaeng_user
POSTGRES_PASSWORD=0P0G3R_gnkX3-5FO8g8IZ8GuUs589vNKd5jfRW-7lQ8
POSTGRES_HOST=localhost
POSTGRES_PORT=5465

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6398

# === External APIs ===

# --- AI (Google Gemini Only) ---
# Delete domains/ai/ if not needed
GEMINI_API_KEY=

# --- Superuser (Auto-created on first run) ---
DJANGO_SUPERUSER_USERNAME=daemon
DJANGO_SUPERUSER_EMAIL=admin@daemon.local
DJANGO_SUPERUSER_PASSWORD=daemonkorea2026

# --- Payments (Portone) ---
# PORTONE_API_KEY=
# PORTONE_SECRET_KEY=

# --- OAuth (Social Login) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# --- Email (SMTP) ---
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=

# --- Monitoring ---
SENTRY_DSN=
LOGFIRE_TOKEN=

# --- Security (Cloudflare Turnstile) ---
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- Storage (Cloudflare R2) ---
# R2_BUCKET=
# R2_ACCESS_KEY=
# R2_SECRET_KEY=
# R2_ENDPOINT_URL=

# === Architecture Note ===
# This project uses HATEOAS (HTML over the wire)
# - No internal JSON API communication
# - Server returns HTML fragments
# - HTMX fetches HTML, not JSON
# - Only external APIs (Gemini, Stripe, OAuth) use JSON
