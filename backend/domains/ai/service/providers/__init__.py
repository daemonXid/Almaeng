"""
🤖 GenAI Module

Generative AI API client for DAEMON-ONE.
Supports Gemini (primary) and OpenAI (secondary).

Usage:
    from domains.genai.interface import GenAIClient

    client = GenAIClient()
    response = client.complete("Hello")
"""
